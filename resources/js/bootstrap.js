// Importações essenciais para o projeto
import 'bootstrap';
import $ from 'jquery';
import Popper from 'popper.js';

// Tornando jQuery globalmente acessível
window.$ = window.jQuery = $;
window.Popper = Popper;

// Outras configurações e inicializações podem ser adicionadas aqui
